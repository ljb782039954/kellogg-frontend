---
export const prerender = true;

import { currentSite } from "@site-package";

const BlogIndexPage = currentSite.pages?.blogIndex;

if (!BlogIndexPage) return Astro.redirect("/404");
---

<BlogIndexPage />
