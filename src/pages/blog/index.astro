---
export const prerender = true;

import { currentSite, loadSitePage } from "@site-package";

const BlogIndexPage = await loadSitePage(currentSite.pages?.blogIndex);

if (!BlogIndexPage) return Astro.redirect("/404");
---

<BlogIndexPage />
