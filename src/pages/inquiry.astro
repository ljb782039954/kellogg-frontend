---
import { currentSite, loadSitePage } from "@site-package";

const InquiryPage = await loadSitePage(currentSite.pages?.inquiry);

if (!InquiryPage) return Astro.redirect("/404");
---

<InquiryPage />
