---
export const prerender = true;

import { currentSite } from "@site-package";

const InquiryPage = currentSite.pages?.inquiry;

if (!InquiryPage) return Astro.redirect("/404");
---

<InquiryPage />
