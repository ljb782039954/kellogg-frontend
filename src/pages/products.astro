---
export const prerender = false;

import { currentSite, loadSitePage } from "@site-package";

const CmsPage = await loadSitePage(currentSite.pages?.products);

if (!CmsPage) return Astro.redirect("/404");
---

<CmsPage pageId="products" />
