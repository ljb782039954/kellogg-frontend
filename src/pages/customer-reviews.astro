---
export const prerender = true;

import { currentSite, loadSitePage } from "@site-package";

const CustomerReviewsPage = await loadSitePage(currentSite.pages?.customerReviews);

if (!CustomerReviewsPage) return Astro.redirect("/404");
---

<CustomerReviewsPage />
