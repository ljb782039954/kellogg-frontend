---
export const prerender = true;

import { currentSite } from "../site-package";

const CustomerReviewsPage = currentSite.pages?.customerReviews;

if (!CustomerReviewsPage) return Astro.redirect("/404");
---

<CustomerReviewsPage />
